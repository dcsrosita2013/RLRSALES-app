# Root env — used by docker-compose to provision PostgreSQL.
# Copy to .env (docker-compose reads it automatically).
POSTGRES_USER=rlr
POSTGRES_PASSWORD=rlr_password
POSTGRES_DB=rlr_db
# Host port for Postgres (5432 may be taken by another local stack).
POSTGRES_PORT=5433
